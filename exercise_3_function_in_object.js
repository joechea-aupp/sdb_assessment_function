// Created a function in an object of userInfo, which will check if user is old enough to vote or not.
// If user is age is greater or equal to 18, it will return true, else false.

const userInfo = {
  name: "John",
  age: 17,
  canVote: function () {
    // your solution here
  },
};

module.exports = userInfo;
